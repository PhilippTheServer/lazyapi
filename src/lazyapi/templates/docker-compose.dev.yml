services:
  {service_name}:
    build:
      context: .
      dockerfile: src/Dockerfile
    image: {service_name}-api:dev
    container_name: {service_name}
    expose:
      - "8000"
    env_file:
      - .env.example
    restart: unless-stopped
